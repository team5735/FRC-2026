 Set-NetConnectionProfile -Name "Unidentified Network" -NetworkCategory Private

IP 10.57.35.2 # roborio
            18 limelight 3g
            17 limelight 3

# deploy code to robot
gradlew deploy  -PteamNumber=5735 --offline  -Dorg.gradle.java.home="C:\Users\Public\wpilib\2026\jdk" 

# Running Tests
Run all tests using the `./gradelw` command from the main repo folder: `FRC-2026`

To run all the tests:
```
./gradlew test
```

Tests results are cached. Tests will only rerun if their source files change. To force rerunning tests:
```
./gradlew test --rerun-tasks
```

To run a specific test:
```
./gradlew test --tests [TestClassPath]

# example:
./gradlew test --tests frc.pathplanner.PathPlannerTest
```

